pipeline {
	agent any
	stages {
    stage('Terraform Plan') {
      steps {
        script {
          dir('module6/sample') {
            // Initialize Terraform
            sh 'terraform init'

            // Run Terraform Plan
            sh 'terraform plan'
          }
        }
      }
    }
	}
}
